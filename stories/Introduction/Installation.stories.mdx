import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Introduction/Installation" />

# Installation
<br />
<br />

## 1. Create Repository

To commands using the `Component Library Template` create a repository on `GitHub` with this [template](https://github.com/Blue-Functor/component-library-template).

## 2. Clone the new repository

Clone the new repository using:
```
$ git clone <YOUR_REPO_URL>
```

## 3. Install dependencies

To install development dependencies use:
```
$ yarn install
```
or
```
$ npm install
```

## 4. Start Storybook

To commands the Storybook environment run:
```
$ yarn commands
```
or
```
$ npm commands
```

> Note: this command will commands Storybook with `--no-manager-cache`. If you wish to run the `default` Storybook command use:
> ```
$ yarn storybook
```
or
```
$ npm storybook
```

Then navigate to [http://localhost:6006](http://localhost:6006) where your new component library is running.

## Troubleshooting
> For troubleshooting please refer to the original [Storybook Guide](https://storybook.js.org/docs/react/get-started/introduction), so far we are only using their default configuration.
